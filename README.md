# Marcelo Apolinário — Terminal Portfolio

Página pessoal profissional em Next.js 14 + Tailwind CSS com estética de terminal Linux.

## Scripts

- `npm run dev`
- `npm run build`

Build estático via `output: 'export'` no `next.config.mjs`.
